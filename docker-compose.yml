services:
  spellsviewer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spellsviewer
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3000}
    volumes:
      - ./logs:/app/logs